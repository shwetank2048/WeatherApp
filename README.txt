#Blockers: Could not consume the openweather API that gives daily forecast for 5 days since it was paid,
 instead had to use openweather API which gave 5 day forecast on 3 hourly basis.

1. CLONE THE PROEJCT.

2. In backend folder unzip the city.list.7z file.

3. run "npm install" inside both folders - Frontend and Backend.

4. For Frontend run "ng serve" to build the project.Open the browser and go to localhost://4200

5. For Backend run "node ." to start the server.

6. Send Req from frontend.